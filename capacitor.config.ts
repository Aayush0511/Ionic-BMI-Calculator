import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sparidtech.first-ionic-app',
  appName: 'BMI Calculator',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
